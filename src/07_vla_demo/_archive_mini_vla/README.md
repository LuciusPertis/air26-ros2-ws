# Archived: the original mini-VLA (toy) demo

Superseded 2026-06-26 by the **real SmolVLA SO-101** demo (`../vla_so101_demo`).

This was the no-GPU, no-torch teaching pipeline: a scripted/pluggable "mini-VLA" →
`/delta_theta` → a toy 3-DOF arm in RViz/Gazebo/MuJoCo (`vla_demo` + `vla_arm_description`).
Kept here for reference (`COLCON_IGNORE` so colcon skips it). To revive it, move the two
package folders back up to `src/07_vla_demo/` and rebuild.
