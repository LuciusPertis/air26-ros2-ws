# Instant query (request/response): are the left/right sides open right now?
# Used by behaviour 3 before deciding how to escape. A SERVICE is the right tool:
# one question, one immediate answer, no ongoing stream.
uint8 threshold       # distance (cm) considered "open"; 0 uses the server default
---
bool left_open
bool right_open
