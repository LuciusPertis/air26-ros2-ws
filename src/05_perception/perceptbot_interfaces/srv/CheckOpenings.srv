# Instant query (request/response): are the left/right sides open right now?
# Used by behaviour 3 before deciding how to escape. A SERVICE is the right tool:
# one question, one immediate answer, no ongoing stream. (Carried over from 02.)
float32 threshold     # distance (m) considered "open"; <=0 uses the server default
---
bool left_open
bool right_open
float32 left_range
float32 right_range
